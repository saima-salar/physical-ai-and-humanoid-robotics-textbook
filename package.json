{
  "name": "physical-ai-humanoid-robotics-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Physical AI and Humanoid Robotics textbook with integrated RAG chatbot",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run start",
    "dev:backend": "cd backend/api && uvicorn main:app --host 0.0.0.0 --port 8001 --reload",
    "build": "npm run build --prefix frontend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "echo 'Backend is a service, no build needed'",
    "setup": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "workspaces": [
    "frontend"
  ]
}